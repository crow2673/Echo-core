#!/bin/bash

# Ensure logs exist
mkdir -p ~/Echo/memory
touch ~/Echo/memory/activity.log

echo "$(date): Golem stopping..." >> ~/Echo/memory/activity.log

systemctl --user stop yagna 2>/dev/null || systemctl stop yagna 2>/dev/null

echo "Golem stopped (or attempted)."
